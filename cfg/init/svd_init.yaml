method: svd
# mean_imputation_strategy: both
svd_params:
  k: 64

# Only for recsys training
# training:
#   use_diag: False
#   freeze_emb: False
#   cutoff: True #False #True #if true: use knee, if False: don't use, if integer: cutoff at that integer
#   invert_order: False